import { TrimChartElementNamePipe } from './trim-chart-element-name.pipe';

describe('TrimChartElementNamePipe', () => {
  it('create an instance', () => {
    const pipe = new TrimChartElementNamePipe();
    expect(pipe).toBeTruthy();
  });
});
