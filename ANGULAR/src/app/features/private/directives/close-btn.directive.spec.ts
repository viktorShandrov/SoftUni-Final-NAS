import { CloseBtnDirective } from './close-btn.directive';

describe('CloseBtnDirective', () => {
  it('should create an instance', () => {
    const directive = new CloseBtnDirective();
    expect(directive).toBeTruthy();
  });
});
